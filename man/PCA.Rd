% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eim-class.R
\name{PCA}
\alias{PCA}
\alias{PCA()}
\title{Reduce Parametric Covariates with PCA}
\usage{
PCA(
  object = NULL,
  X = NULL,
  W = NULL,
  V = NULL,
  json_path = NULL,
  components = NULL,
  sd_threshold = NULL,
  center = TRUE,
  scale = TRUE
)
}
\arguments{
\item{object}{An object of class \code{eim}, which can be created using the \link{eim} function.}

\item{X}{A \verb{(b x c)} matrix representing candidate votes per ballot box.}

\item{W}{A \verb{(b x g)} matrix representing group votes per ballot box.}

\item{V}{A \verb{(b x a)} matrix with parametric covariates.}

\item{json_path}{A path to a JSON file containing \code{X}, \code{W}, and \code{V} fields.}

\item{components}{Integer specifying the number of principal components to keep.}

\item{sd_threshold}{Numeric in \verb{(0, 1]} indicating the minimum cumulative proportion
of variance explained by the retained components.}

\item{center}{Logical indicating whether to center the columns of \code{V} before PCA.}

\item{scale}{Logical indicating whether to scale the columns of \code{V} before PCA.}
}
\value{
Returns an \code{eim} object with the \code{V} matrix replaced by its PCA scores. The
columns of \code{V} are renamed as \verb{PCA 1}, \verb{PCA 2}, ..., up to the chosen number
of components.
}
\description{
Applies a Principal Component Analysis (PCA) to the covariates matrix \code{V} and
replaces it with a lower dimensional representation. This function is intended
for parametric workflows and requires a valid \code{V} matrix.
}
\examples{
sim <- simulate_election(
    num_ballots = 50,
    num_candidates = 3,
    num_groups = 2,
    ballot_voters = 40,
    parametric = TRUE,
    num_attributes = 10,
    num_districts = 2,
    seed = 1
)

sim_pca <- PCA(sim, components = 2)
sim_pca$V

}
