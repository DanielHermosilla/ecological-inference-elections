% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eim-S3.R
\name{plot.eim}
\alias{plot.eim}
\title{Plot estimated probabilities}
\usage{
\method{plot}{eim}(
  x,
  title = "Estimated probabilities",
  legend_title = "Candidates",
  color_scale = NULL,
  min_pct = 3,
  ...
)
}
\arguments{
\item{x}{An "eim" object.}

\item{title}{Title for the plot.}

\item{legend_title}{Title for the legend.}

\item{color_scale}{A vector of colors or a palette for the candidates.}

\item{min_pct}{Minimum percentage required to display a label.}

\item{...}{Additional arguments that are ignored.}
}
\value{
Returns a \code{ggplot2} object representing the pie charts.
}
\description{
Plots the estimated probabilities as pie charts using \code{ggplot2}, one per row of the probability matrix.
Each slice displays its percentage label. For the parametric case, it does a weighted average over groups to retrieve the global probabilities.
}
\examples{
\donttest{
sim <- simulate_election(
    num_ballots = 100,
    num_candidates = 4,
    num_groups = 5,
    ballot_voters = 40,
    parametric = TRUE,
    num_attributes = 2,
    num_districts = 2,
    seed = 42
)
fit <- run_em(sim, maxiter = 5)

plot(fit, title = "Estimated probabilities", legend_title = "Candidates", min_pct = 7)
}
}
